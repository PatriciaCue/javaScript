/**
 * DECLARACION DE VARIABLES
 * Las CONSTANTES tienen que estar inicializadas
 * y no pueden cambiar el valor durante el programa
 */
const carId = 100;

/**
 * LET VS VAR
 * Diferencias:
 * Cuando usamos LET en un buble no se puede llamar fuera de ese bucle
 * No se puede llamar antes a la variable si no se ha creado da ERROR
 * 
 * VAR se puede usar fuera del bucle se declara de manera global
*/

//console.log(carID); // me va a dar un ERROR la variable se tiene que declarar antes
let carID =42;

console.log(carNum); // me devuelve UNDEFINED
let carNum =42;


console.log('Hello World!');
